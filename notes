when sending requests u are likely to get 403 error, this is because u need Referer header that starts with http://<router IP>, the Referer header is case sensitive and will NOT work with nodes http library as that appears to lowercase all headers